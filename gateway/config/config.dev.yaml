server:
  host: "0.0.0.0"
  port: 8080

database:
  host: localhost
  port: 5432
  user: agentura
  password: agentura_dev
  name: agentura_gateway
  ssl_mode: disable

agent:
  grpc_addr: "localhost:50051"

auth:
  enabled: false

logging:
  level: debug
  format: text
