domain:
  name: hr
  description: "Human Resources operations and talent management"
  owner: "hr-team"

skills:
  - name: leave-policy
    path: leave-policy
    role: specialist
    model: anthropic/claude-haiku-4-5-20251001
    cost_budget: "$0.01"
    triggers:
      - type: command
        pattern: "leave policy"
      - type: command
        pattern: "how many leaves"

guardrails:
  budget:
    max_per_execution: "$0.01"
    alert_at: "$0.008"

feedback:
  capture_corrections: true
  auto_generate_tests: true
  test_framework: deepeval
