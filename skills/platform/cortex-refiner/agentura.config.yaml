domain:
  name: platform
  description: "SKILL.md refinement from user feedback"

skills:
  - name: cortex-refiner
    role: specialist
    model: anthropic/claude-sonnet-4.5
    trigger: manual
    cost_budget: "$0.10"
    timeout: "60s"

guardrails:
  max_cost_per_execution: "$0.20"
  human_in_loop:
    require_approval: false

display:
  title: "Polish"
  subtitle: "Skill Refiner"
  avatar: "SR"
  color: "#6366f1"
  tags: ["Skill Refinement", "QA"]
