domain:
  name: platform
  description: "PM interview persona for cortex skill creation"

skills:
  - name: cortex-interviewer
    role: specialist
    model: anthropic/claude-haiku-4.5
    trigger: manual
    cost_budget: "$0.05"
    timeout: "30s"

guardrails:
  max_cost_per_execution: "$0.10"
  human_in_loop:
    require_approval: false

display:
  title: "Sage"
  subtitle: "Skill Discovery PM"
  avatar: "SI"
  color: "#6366f1"
  tags: ["PM Interview", "Skill Design"]
