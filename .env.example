# Required: at least one LLM API key
# ANTHROPIC_API_KEY=sk-ant-...
OPENROUTER_API_KEY=your-openrouter-key-here

# Optional: PostgreSQL (enables persistent memory store)
# DATABASE_URL=postgresql://agentura:agentura@localhost:5432/agentura

# Optional: Gateway URL (for CLI gateway commands)
# AGENTURA_GATEWAY_URL=http://localhost:3001
