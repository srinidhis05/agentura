apiVersion: v1
kind: Service
metadata:
  name: executor
  namespace: aspora
  labels:
    app: executor
spec:
  type: ClusterIP
  selector:
    app: executor
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
